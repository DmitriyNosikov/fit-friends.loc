// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// npx prisma migrate dev \
// --name "Added model for Product" \
// --schema ./backend/src/app/libs/models/prisma/schema.prisma \
// --skip-generate

generator client {
  provider = "prisma-client-js"
  output = "../../../../../../node_modules/.prisma/client/"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum PaymentType {
  visa
  mir
  iomoney
}

//////////////////// USERS ////////////////////
model User {
  id                String    @id @default(uuid()) @unique
  createdAt         DateTime? @default(now()) @map("created_at")
  updatedAt         DateTime? @updatedAt @map("updated_at")

  name              String
  email             String    @unique
  passwordHash      String    @map("password_hash")
  avatar            String?
  gender            String
  birthDate         DateTime?  @map("birth_date")
  description       String?
  location          String 
  pageBackground    String?    @map("page_background")

  role              String?
  level             String?
  trainingType      String[]  @map("training_type")
  trainingDuration  String?   @map("training_duration")
  loseCaloriesLimit Int?      @map("lose_calories_limit")
  dayCaloriesLimit  Int?      @map("day_calories_limit")
  isReadyToTraining Boolean?  @map("is_ready_to_training")

  certificates      String[]
  friendsList       String[]  @map("friends_list")

  // Relations
  training Training[]
  balance Balance[]
  order Order[]
  trainingReview TrainingReview[]
  refreshToken RefreshToken[]

  friends  User[]  @relation("User_friends")
  symmetricFriends  User[]  @relation("User_friends")

  Request_InitiatorUser Request[] @relation("initiatorUser")
  Request_targetUser Request[] @relation("targetUser")

  // It works, but we can`t get related fields with request (initiatorUser & targetUser)
  // Request_InitiatorUser Request[] @relation(name: "user_initiator")
  // Request_targetUser Request[] @relation(name: "user_target")

  @@map("users")
}

//////////////////// REQUESTS ////////////////////
model Request {
  id              String    @id @default(uuid())
  createdAt       DateTime? @default(now()) @map("created_at")
  updatedAt       DateTime? @updatedAt @map("updated_at")

  requestType     String    @map("request_type")
  initiatorUserId String    @map("initiator_user_id")
  targetUserId    String    @map("target_user_id")
  status          String

  initiatorUser User @relation(name: "initiatorUser", fields: [initiatorUserId], references: [id])
  targetUser User @relation(name: "targetUser", fields: [targetUserId], references: [id])

  // It works, but we can`t get related fields with request (initiatorUser & targetUser)
  // initiatorUser User[] @relation(name: "user_initiator")
  // targetUser User[] @relation(name: "user_target")
  
  @@map("requests")
}

//////////////////// TRAINIGNS ////////////////////
model Training {
  id                String    @id @default(uuid())
  createdAt         DateTime? @default(now()) @map("created_at")
  updatedAt         DateTime? @updatedAt @map("updated_at")

  title             String
  background        String
  userLevel         String
  trainingType      String    @map("training_type")
  trainingDuration  String    @map("training_duration")
  price             Int
  discount          Int?
  calories          Int
  description       String
  gender            String
  video             String
  rating            Int?
  trainersName      String?    @map("trainers_name")
  isSpecial         Boolean?  @map("is_special")
  userId            String    @map("user_id")

  balance           Balance[]
  order             Order[]
  review            TrainingReview[]
  user              User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("trainings")
}

//////////////////// ORDERS ////////////////////
model Order {
  id                      String      @id @default(uuid())
  createdAt               DateTime?   @default(now()) @map("created_at")
  updatedAt               DateTime?   @updatedAt @map("updated_at")

  type                    String
  userId                  String      @map("user_id")
  trainingId              String      @map("training_id")
  price                   Int
  trainingsCount          Int         @map("trainings_count")
  totalPrice              Int         @map("total_price")
  paymentType             PaymentType @map("payment_type")

  training Training @relation(fields: [trainingId], references: [id], onDelete: Cascade)
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("orders")
}

//////////////////// BALANCE ////////////////////
model Balance {
  id                      String      @id @default(uuid())
  createdAt               DateTime?   @default(now()) @map("created_at")
  updatedAt               DateTime?   @updatedAt @map("updated_at")

  trainingId              String      @map("training_id")
  userId                  String      @map("user_id")
  remainingTrainingsCount Int         @map("remaining_trainings_count")

  hasTrainingStarted      Boolean?     @map("has_training_started")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  training Training @relation(fields: [trainingId], references: [id], onDelete: Cascade)

  @@map("balances")
}

//////////////////// REVIEWS ////////////////////
model TrainingReview {
  id         String      @id @default(uuid())
  createdAt  DateTime?   @default(now()) @map("created_at")
  updatedAt  DateTime?   @updatedAt @map("updated_at")

  userId     String      @map("user_id")
  trainingId String      @map("training_id")
  rating     Int
  text       String

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  training Training @relation(fields: [trainingId], references: [id], onDelete: Cascade)
  
  @@map("training_reviews")
}

//////////////////// TOKENS ////////////////////
model RefreshToken {
  id        String    @id @default(uuid())
  createdAt DateTime? @default(now()) @map("created_at")
  updatedAt DateTime? @updatedAt @map("updated_at")

  expiresIn DateTime? @map("expires_in")
  tokenId   String    @map("token_id")
  userId    String    @map("user_id")

  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("refresh_token")
}